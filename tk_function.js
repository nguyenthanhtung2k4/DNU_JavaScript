function tesst() {
     var so=''
     for (var tung of  arguments) {
         so +=`${tung} : `
     }
      console.log(so);
}
tesst('a','b',123,true,'c')
/* sử dụng  gần như python khác nhau  một  chút  về cách  khai  báo biến  sd (var)